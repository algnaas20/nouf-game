# سجل القرارات — تأسيس-المشروع

قرارات بصيغة `D-xx`: السؤال، الخيارات، التوصية، والحسم. **قرارات المستخدم تُقتبس بنصها العربي كما قالها.**

| # | القرار | الخيارات | التوصية | الحسم |
|---|---|---|---|---|
| D-01 | طريقة اللعب: أجهزة منفصلة أونلاين أم شاشة واحدة مشتركة | أونلاين / شاشة واحدة / شاشة واحدة بمعمارية تسمح بالأونلاين لاحقًا | شاشة واحدة — أبسط وأسرع لأول نسخة تُلعب | **المستخدم (2026-08-07):** «يكون من شاشة وحدة يلعبون مع بعض واحد يستعرضه في مجلس أو مكان عائلي في البيت أو الأصدقاء» |
| D-02 | المنصة | ويب / سطح مكتب ويندوز / جوال | ويب — أسهل للوسائط وللمشاركة | **المستخدم (2026-08-07):** ويب — متصفح |
| D-03 | الاستضافة والتسليم | حزمة يرفعها المستخدم على استضافة ثابتة / غيرها | GitHub Pages أو مشابه | **المستخدم (2026-08-07):** «برفعة على github أو هوست غيره يكون ملف جاهز أرفعه بنفسي» — ويؤكَّد أن الاستضافة الثابتة تقبل كل الملفات الثابتة لا HTML فقط |
| D-04 | محرّر الأسئلة | داخل اللعبة برفع الوسائط / ملفات يحرّرها المستخدم يدويًا / الاثنان | محرّر داخل اللعبة | **المستخدم (2026-08-07):** محرّر داخل اللعبة برفع الوسائط |
| D-05 | حجم أول نسخة | نسخة تُلعب من أولها لآخرها / نسخة كاملة بكل المزايا | نسخة تُلعب كاملة وصغيرة | **المستخدم (2026-08-07):** نسخة تُلعب من أولها لآخرها |
| D-06 | وضع العمل: متابعة كل مرحلة أم تشغيل كامل | متابعة كل مرحلة / تشغيل كامل / متابعة القرارات الكبيرة فقط | متابعة أول عرض ثم تشغيل كامل | **المستخدم (2026-08-07):** «اشتغلوا بدون رجوع لي» — وضع التشغيل الكامل مفعَّل من 2026-08-07. كل قرار يطرأ يفصل فيه المستشار القراري المعني ويُسجَّل هنا، والمستخدم لا يُوقَف عمله بانتظار جواب |
| D-07 | أين تُحفظ الوسائط، وكيف تُنقل اللعبة بين الأجهزة (حزمة تصدير/استيراد) | يفصل فيه `durability-advisor` بناءً على تحقيق `media-storage-expert` | — | بانتظار حسم المستشار (وضع التشغيل الكامل — D-06) |
| D-08 | الفيديو الثقيل: مرفوع مع الحزمة أم رابط خارجي أم الاثنان | يفصل فيه `durability-advisor` | — | بانتظار حسم المستشار (D-06) |
| D-09 | شكل المتاهة وطريقة التقدّم وطريقة تناوب الأسئلة | يفصل فيه `play-experience-advisor` بناءً على تحقيق `game-systems-expert` | — | بانتظار حسم المستشار (D-06) |
| D-10 | حدود النسخة الأولى: ما يدخلها وما يُؤجَّل | يفصل فيه `scope-advisor` | — | بانتظار حسم المستشار (D-06) |
| D-12 | الكلفة: هل يُقبل أي خيار مدفوع (استضافة، تخزين، خدمة وسائط)؟ | مجاني بالكامل / مجاني الآن ومدفوع عند التوسع / يُقبل مدفوع رخيص | مجاني بالكامل — قيد ملزم على كل قرار لاحق | **المستخدم (2026-08-07):** «أهم شي كل شي مجاني ما أبي أدفع إذا برفع اللعبة أونلاين» — **قيد حاكم**: أي خيار يكلّف مالًا الآن أو عند النمو مرفوض، لا «موصى به مع تنويه». يشمل: استضافة، تخزين سحابي، خدمات ترميز فيديو، نطاق مدفوع |
| D-13 | خصوصية وسائط العائلة مقابل مجانية الاستضافة | نشر كل شيء علنًا / نشر المحرّك فقط والوسائط على جهاز المستخدم / تشغيل محلي بالكامل | كان الترجيح: المحرّك فقط | **المستخدم (2026-08-07) — محسوم:** «عادي أي أحد يدخل اللعبة أبيه للعامة». نُبِّه صراحةً أن المستودع العام يعني أن ملفات الوسائط قابلة للتنزيل لمن يملك الرابط، وأعاد تأكيد رغبته. ⟵ **اللعبة عامة ومنشورة بأسئلتها**. الأثر التقني المتبقي: حدود 1GB و100GB/شهر ترجع قيودًا فعلية على الفيديو (تُدار في D-08)، ويبقى مسار «استورد حزمتك الخاصة» قائمًا لمن يريد لعبته الخاصة بلا نشر |
| D-14 | اسم اللعبة | — | — | **المستخدم (2026-08-07):** «**لعبة نوف**» — هذا الاسم المعتمد في الواجهة وعنوان الصفحة والرابط. (اسم مجلد المشروع على القرص يبقى `لعبة نوف المحبوب` كما هو — لا يُعاد تسميته تفاديًا لكسر المسارات) |
| D-22 | نموذج التجهيز والنشر — **يُبطل D-20 و D-21 ويعيد العمل بـD-13** | تجهيز قبل المسابقة ونشر كامل / رفع الحزمة وقت اللعب | — | **المستخدم (2026-08-07) — حاسم ونهائي:** «لا أبي أرفع اللعبة وأجهزها قبل المسابقة وخلاص». ⟵ **المؤسِّس يجهّز اللعبة كاملة بأسئلتها ووسائطها في المتصفح قبل الليلة، ثم ينشرها. ليلة المسابقة: يفتح الرابط ويلعب.** لا تحميل حزم وقت اللعب كمسار أساسي. **ويبقى «احفظها في الكمبيوتر»** نسخةً احتياطية ولنقل اللعبة بين أجهزته — لا كآلية تسليم |
| D-23 | الفيديو | يُدعم / يُلغى | — | **المستخدم (2026-08-07): ملغى نهائيًا.** «ألغِ فكرة الفيديو». أنواع الوسائط المدعومة: **نص وصورة وصوت فقط**. ⟵ **يسقط معه**: حد ١٠٠ ميجا للملف، وفخ ترميز HEVC (فيديو الآيفون يعمل عنده ويظهر أسودًا على لابتوب المجلس)، واعتماد التشغيل على استجابات النطاق الجزئي، وخطر تفجير الذاكرة، وحساب دقائق الفيديو كله. الصور والأصوات تدخل المجاني بأريحية تامة |
| ~~D-20~~ | ~~ما الذي يُنشر على الرابط~~ — **مُبطل بـD-22 (2026-08-07)**؛ يبقى مقيّدًا للسجل لا للعمل به | نشر اللعبة بأسئلتها / نشر المحرّك فقط والمحتوى يُرفع وقت اللعب | — | **المستخدم (2026-08-07) — حاسم:** «اللي يلعب يرفعها وقت اللعبة بس، بعدها تروح، ما تنحفظ للأبد بقاعدة بيانات. بس حط حفظ لها في الكمبيوتر لو أبي ما أفقدها». ⟵ **الرابط عام ويفتحه أي أحد (D-13 قائم)، لكن المنشور هو المحرّك فقط بلا أي محتوى شخصي.** كل من يلعب يحمّل حزمته من جهازه وقت اللعب، وتُستعمل للجلسة ثم تزول. **لا قاعدة بيانات ولا حفظ دائم على الخادم.** ويبقى **«احفظها في الكمبيوتر»** المسار الوحيد الدائم لمن لا يريد أن يفقد عمله |
| D-21 | أثر D-20 على الحدود والميزانيات | — | — | **سقطت ميزانيات النشر كلها**: حد ٢٥ ميجا للرفع، وحد ١٠٠ ميجا للملف، وسقف ١ جيجا للموقع، وحساب دقائق الفيديو — كلها بلا معنى الآن لأن **لا وسائط تُرفع أصلًا**. الموقع يبقى صغيرًا (المحرّك فقط)، والنطاق الترددي يصير مهملًا. حجم الوسائط يحدّه **قرص المستخدم وذاكرة متصفحه فقط** ⟵ الفيديو الطويل صار ممكنًا. **الخطر التقني الأول انتقل** إلى: تشغيل حزمة كبيرة محليًا بلا تفجير الذاكرة أثناء جلسة طويلة |
| D-09.1 | نموذج التناوب وشعور «الفريق يوجّه السؤال» | — | — | **حسمه `play-experience-advisor`**: التطبيق يسحب السؤال من رزمة واحدة والدور يتناوب على **الإجابة**؛ و«التوجيه» يصير وظيفة حقيقية: **الفريق الذي ليس دوره يقرأ السؤال بصوته من الشاشة**، واسمه معروض في كل جولة. بلا ضغطة إضافية (ضغطتان للسؤال). يفرض: **الخيار الصحيح لا يُميَّز بصريًا قبل الكشف** (القارئ يرى الشاشة)، وحقول `category`/`difficulty` تُحجز من اليوم الأول ولو معطّلة |
| D-09.2 | شكل المتاهة | — | — | **حسمه `play-experience-advisor`**: مسار مرسوم متطابق للفريقين (`الموقع = عدد الإجابات الصحيحة` — المتاهة تُقرأ كلوحة نتائج من بعيد)، **وطابع المتاهة الحقيقي** (جدران، زوايا، أركان مغلقة) يعيش **خارج ممر السير**. قاعدة فنية ملزمة: **الطريق المسدود الزخرفي لا يجوز أن يبدو كطريق كان ممكنًا** — أول ما يسأل أحد «ليش ما راحوا من هناك؟» ينكسر المعنى |
| D-09.3 | عدالة النهاية (أفضلية الفريق البادئ ≈٥٥٫٧٪) | — | — | **حسمه `play-experience-advisor`**: لا تنتهي الجولة إلا وقد تساوى عدد محاولات الفريقين، **ويُعلن ذلك عند الإعداد**، و**لا يُحتفل بأي فوز عند وصول الأول** (الاحتفال ثم سحبه هو الشيء الوحيد الذي يجعلها تبدو مدبَّرة). الفاصل يُلعب **أزواجًا** (سؤال مختلف لكل فريق في نفس الجولة) بترتيب معكوس — لا موت مفاجئ. تكراره ~١١٪ عند إتقان ٠٫٧ — لا «دائمًا» |
| D-09.4 | المؤقت | — | — | **حسمه `play-experience-advisor`: لا مؤقت ولا أي ساعة على الشاشة** في النسخة الأولى. الأزمنة تُسجَّل بصمت في سجل الأحداث فيبقى الباب مفتوحًا بلا أي واجهة |
| D-09.5 | طول الجولة ومعالجة نفاد الأسئلة | — | — | **حسمه `play-experience-advisor`**: ٦ / ١٠ / ١٤ محطة، **والتطبيق يختار المناسب من رزمتك تلقائيًا** بثلاث حالات (أخضر/تنبيه/رفض) فلا يحسب المستعرض شيئًا. وعند نفاد الأسئلة **لا يكتفي التطبيق برسالة**: المتقدّم يفوز باحتفال كامل، وعند التعادل تظهر **«سؤال من الحضور»** — شخص محايد يسأل بصوته — وزر ثالث «نعلنها تعادل» يجعل التعادل خيار الغرفة لا حكم الشاشة. ومؤشّر «باقي ٦ أسئلة» يظهر تحت الثمانية |
| D-09.6 | الفريق المتأخّر بفارق كبير | — | — | **حسمه `play-experience-advisor`: لا آلية تعويض**. «بقي X خطوات» ولا تُعرض نسبة فوز أبدًا، والفريق المتأخّر يحتفظ بدور القراءة، **ولا عبارات مواساة** (الشفقة أمام الأهل أسوأ من الخسارة)، **والجولة الجديدة تبدأ بالفريق الخاسر** — مجانية بحكم قاعدة تساوي المحاولات وتُقرأ كرم |
| D-09.7 | الوسائط قبل الخيارات | — | — | **حسمه `play-experience-advisor`**: في الصوت والفيديو تُخفى الخيارات أثناء أول تشغيل وتظهر تلقائيًا عند انتهائه، مع زر **«اعرض الخيارات»** حاضر دائمًا فلا تتجمد الشاشة لو فشل الإشعار. النص والصورة: الخيارات فورًا |
| D-15 | المضيف المجاني المعتمد | GitHub Pages / Cloudflare Pages / Netlify | — | **حسمه `static-delivery-expert` (2026-08-07)**: **GitHub Pages** أساسًا و**Cloudflare Pages** بديلًا جاهزًا. الاثنان مجانيان بلا بطاقة و**الفاتورة مستحيلة بنيويًا** (لا محاسبة على الاستهلاك). **Netlify مرفوض** — سلوكه عند تجاوز الحد: تعليق الموقع ودفع لترقية. النطاق المخصص مرفوض (كلفة متكررة). محفّز الانتقال لـCloudflare مكتوب مسبقًا: رسالة من GitHub أو شهر يتجاوز 50GB ⟵ ٥ خطوات بنفس الملفات (Cloudflare بلا سقف نطاق ترددي أصلًا) |
| D-16 | قواعد المسارات وأسماء الملفات (تمنع «يشتغل عندي و404 بعد الرفع») | — | — | **حسمه `static-delivery-expert`**: مسارات نسبية بالكامل (`base: './'`)، و**ممنوع أي مسار يبدأ بـ`/`** (بوابة بناء لا عادة مراجعة)، وكل اسم ملف يُنتَج **لاتيني صغير فقط** `^[a-z0-9._-]+$` — القاتل ليس الترميز بل توحيد يونيكود (NFC/NFD) الذي ينتج 404 على ملف موجود أمامك. العربية داخل الملفات لا في الروابط. اسم المستودع لاتيني (`nouf-game`) والاسم المعروض «لعبة نوف». **وحساسية حالة الأحرف لا يكشفها خادم ويندوز محلي إطلاقًا** — تُثبَت بتدقيق نصي مطابق حرفيًا |
| D-17 | ميزانية الوسائط داخل المجاني | — | — | **حسمه `static-delivery-expert`**: القيد الحاكم فعليًا ليس حد الجيجا بل **٢٥ ميجا للملف الواحد** عند الرفع من موقع GitHub، و**١٠٠ ملف لكل دفعة**. السقف: ٣٠٠ ميجا للعبة كاملة، والتنبيه عند ٢٠٠. داخل ٢٠٠ ميجا: **٢٠٠ سؤال بصور + ١٠٠ سؤال بصوت + ~٥ دقائق فيديو إجمالًا**. الفيديو ≈ ١٩ ميجا للدقيقة (720p) وهو **الصنف الوحيد القادر على كسر أي حد**. إلزامي: `preload="none"` على كل وسائط — يرفع سقف الجلسات الشهرية من ~٥٣٠ إلى ~١٠٢٠٠ |
| D-18 | تشغيل اللعبة من مجلد على الجهاز مباشرة (`file://`) كمسار أساسي | يُقبل / يُرفض | — | **حسمه `static-delivery-expert`: مرفوض** — يفرض نسخة بناء ثانية، ويضخّم الحجم ~٣٣٪، و**تخزين المتصفح غير مضمون على `file://`** فيصير المحرّر نفسه على أرضية قد لا تحفظ. البديل الذي يعطي نفس فائدة العمل بلا إنترنت **بصفر خطوات إضافية**: تطبيق ويب قابل للتثبيت (PWA) بنفس الملفات |
| D-19 | مساران للمحتوى: لعبة منشورة + استيراد حزمة خاصة | واحد فقط / الاثنان | — | **حسمه `static-delivery-expert`: الاثنان في نفس الحزمة** عبر مُحمِّل مرتّب (حزمة مستوردة ⟵ ملف يختاره المستخدم ⟵ الحزمة المنشورة) — بلا نسخة بناء ثانية. المستخدم يعمل **ضغطتين** لتحميل حزمته في المجلس، و**صفر** لو كانت محفوظة على الجهاز |
| D-11 | عائلة التقنية (HTML/CSS/JS خام أم Vite+TS أم Vite+إطار خفيف) | يحسمها المخطِّط بناءً على تقارير الخبراء وقيود الاستضافة الثابتة | — | بانتظار المخطِّط |
